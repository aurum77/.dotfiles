### Variable
#
# Logo key. Use Mod1 for Alt.
set $mod Mod1

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Set border size
set $bordersize 2

# Set alacritty as default
set $term alacritty

# Start a terminal
bindsym $mod+Return exec $term

# Start a floating terminal
bindsym $mod+Ctrl+Return exec $term --title=floating_terminal

# Move floating windows using alt and left click,
# resize them with right click
floating_modifier $mod normal

# Reload the configuration file
bindsym $mod+Shift+c reload

#
# Moving around:
#
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left 40px
    bindsym $mod+Shift+$down move down 40px
    bindsym $mod+Shift+$up move up 40px
    bindsym $mod+Shift+$right move right 40px

#
# Workspaces:
#
    # Set workspaces with names
    set $ws1 "1"
    set $ws2 "2"
    set $ws3 "3"
    set $ws4 "4"
    set $ws5 "5"
    set $ws6 "6"
    set $ws7 "7"
    set $ws8 "8"
    set $ws9 "9"

    # Switch to workspace
    bindsym $mod+1 workspace $ws1
    bindsym $mod+2 workspace $ws2
    bindsym $mod+3 workspace $ws3
    bindsym $mod+4 workspace $ws4
    bindsym $mod+5 workspace $ws5
    bindsym $mod+6 workspace $ws6
    bindsym $mod+7 workspace $ws7
    bindsym $mod+8 workspace $ws8
    bindsym $mod+9 workspace $ws9

    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace $ws1
    bindsym $mod+Shift+2 move container to workspace $ws2
    bindsym $mod+Shift+3 move container to workspace $ws3
    bindsym $mod+Shift+4 move container to workspace $ws4
    bindsym $mod+Shift+5 move container to workspace $ws5
    bindsym $mod+Shift+6 move container to workspace $ws6
    bindsym $mod+Shift+7 move container to workspace $ws7
    bindsym $mod+Shift+8 move container to workspace $ws8
    bindsym $mod+Shift+9 move container to workspace $ws9

#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent



include /etc/sway/config.d/*

# My configurations
# -----------------

# Window rules
for_window [title=".*"] border pixel $bordersize

for_window [instance="Places"] floating enable, resize set width 1200, resize set height 800


for_window [title=floating_terminal|UltimMC|cpupower-gui] floating enable
for_window [title="File Operation Progress"] floating enable
for_window [title="on QEMU/KVM$"] floating enable
for_window [title="Virtual Machine Manager"] floating enable, resize set width 800, resize set height 600

for_window [class="(jetbrains|java-util)-(.*)" title="win0"] floating enable
for_window [class="superProductivity"] floating enable

for_window [app_id=Thunar|thunar|mpv|gnome-pomodoro] floating enable
for_window [app_id=pavucontrol] floating enable, resize set width 800
for_window [app_id=zenity] floating enable

# Use wofi as app launcher
set $menu wofi --term=alacritty --show=drun --insensitive --allow-images --width=20% --lines=12 --no-actions --hide-scroll --prompt="Search for Applications"
bindsym $mod+d exec $menu

# Use wofi for scripts
bindsym $mod+Period exec $HOME/.config/wofi/scripts/wofi-emoji.sh
bindsym $mod+Shift+q exec $HOME/.config/wofi/scripts/wofi-power.sh

# Kill focused window
bindsym $mod+q kill

# Set gruvbox colorscheme colors
set {
  $bg #282828
  $bg_alpha #282828F2
  $red #cc241d
  $green #98971a
  $yellow #d79921
  $blue #458588
  $purple #b16286
  $aqua #689d68
  $orange #d65d0e
  $gray #a89984
  $darkgray #1d2021
  $white #fbf1c7
}

# Set wallpaper
output * bg $HOME/pics/a-frame-wood-cabin-in-forest.jpg fill

bar {
    # Set the position of the bar
    position bottom

    # Set bar gaps
    gaps 8px

    # Modifier to show bar
    modifier $mod

    # When the status_command prints a new line to stdout, i3status updates.
    status_command exec i3status -c $HOME/.config/i3status/i3status.conf

    # Hide workspace numbers
    strip_workspace_numbers no

    # Set some colors
    colors {
        # bar background color
        background $bg_alpha
        # text color used for blocks that do not have a color specified.
        statusline $yellow
        # workspaces section
        #                    border  backgr. text
        focused_workspace    $aqua $aqua $darkgray
        inactive_workspace   $bg_alpha $bg_alpha $yellow
        active_workspace     $darkgray $darkgray $yellow
        urgent_workspace     $red $red $bg
    }
}

# Blue gruvbox
# class                 border|backgr|text|indicator|child_border
client.focused          $blue $blue $darkgray $yellow $blue
client.focused_inactive $darkgray $darkgray $yellow $orange $darkgray
client.unfocused        $darkgray $darkgray $yellow $orange $darkgray
client.urgent           $red $red $white $red $red

# Colored window borders
default_border pixel $bordersize
smart_borders on

# Set gaps
gaps outer 8
gaps bottom 0

# Set i3status font
font pango:Iosevka Nerd Font Medium 11

# Set input language
input type:keyboard {
    xkb_layout tr(alt)
    xkb_model "pc101"
}

# Enable tap to click and natural scroll
input type:touchpad {
    tap enabled
    natural_scroll enabled
    drag_lock enabled
}

# Disable mouse accel
input type:pointer {
    accel_profile flat
    pointer_accel 0
}

# Make the cursor move between focused containers
mouse_warping container

# Bind the laptop context menu button to start thunar
bindcode 135 exec thunar

# Screenshots, requires wl-clipboard and jq
bindsym $mod+Shift+s exec grim -g "$(slurp -d)" - | wl-copy
bindsym Print exec grim -o $(swaymsg -t get_outputs | jq -r '.[] | select(.focused) | .name')

# Book search
bindsym $mod+Shift+b exec find $(xdg-user-dir DOCUMENTS)/books -not -path '*/.*' | tail -n +2 | sed 's|/home/borab/docs/books/||g;' | wofi --show=dmenu --insensitive --no-actions --hide-scroll --lines=10 --width=30% --cache-file=/dev/null --prompt="Search for Books" | sed 's|\ |\\ |g' | xargs -I{dir_or_file} exo-open \"${HOME}/docs/books/{dir_or_file}\"

# Recording bind
bindsym $mod+Shift+r exec sh $HOME/.config/sway/scripts/recorder.sh

# Set themes (not all gtk applications abide by the theme, so I use xsettingsd as well)
set $gnome-schema org.gnome.desktop.interface
exec_always {
    gsettings set $gnome-schema gtk-theme 'gruvbox-dark-gtk'
    gsettings set $gnome-schema icon-theme 'gruvbox-dark-icons-gtk'
    gsettings set $gnome-schema cursor-theme 'capitaine-cursors'
    gsettings set $gnome-schema font-name 'Iosevka Nerd Font'
}

# Set seat cursor theme
seat seat0 xcursor_theme "capitaine-cursors"

# Reload xwindow themes with sway reload
exec_always xsettingsd

# Resizing containers:
mode "resize" {
    bindsym $left resize shrink width 25px
    bindsym $down resize shrink height 25px
    bindsym $up resize grow height 25px
    bindsym $right resize grow width 25px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# Scratchpad binds
bindsym $mod+Shift+tab move scratchpad
bindsym $mod+tab scratchpad show

# Fast workspace switch binds
bindsym $mod+x workspace next_on_output
bindsym $mod+z workspace prev_on_output

# Brightness control using brightnessctl
bindsym XF86MonBrightnessUp exec brightnessctl set +20
bindsym XF86MonBrightnessDown exec brightnessctl set 20-

# Volume and media control
bindsym XF86AudioRaiseVolume exec $HOME/.config/sway/scripts/sound.sh up
bindsym XF86AudioLowerVolume exec $HOME/.config/sway/scripts/sound.sh down
bindsym XF86AudioMute exec $HOME/.config/sway/scripts/sound.sh mute
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# CPU Governor control using cpupower-gui
bindsym $mod+Ctrl+b exec cpupower-gui pr Conservative
bindsym $mod+Ctrl+p exec cpupower-gui pr Performance

# Lock on lid close
set $lock 'swaylock -i $HOME/pics/a-frame-wood-cabin-in-forest.jpg'
bindswitch --reload --locked lid:on exec $lock

# Night light
exec gammastep -l 41:28 -t 6700:4000

# Start keyring daemon
exec eval $(gnome-keyring-daemon --start)
